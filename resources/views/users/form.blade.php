@extends('layouts.app') @section('content')<div class="top"><div><h1>{{$user->exists?'Edit User':'Add User'}}</h1></div></div><div class="card"><form method="POST" action="{{$user->exists?route('users.update',$user):route('users.store')}}">@csrf @if($user->exists)@method('PUT')@endif<div class="form-grid"><div class="field"><label>Username</label><input class="input" name="username" value="{{old('username',$user->username)}}" required></div><div class="field"><label>Role</label><select name="role"><option value="OWNER" @selected(old('role',$user->role)==='OWNER')>Owner</option><option value="SALES_CLERK" @selected(old('role',$user->role)==='SALES_CLERK')>Sales Clerk</option></select></div><div class="field"><label>First Name</label><input class="input" name="first_name" value="{{old('first_name',$user->first_name)}}"></div><div class="field"><label>Last Name</label><input class="input" name="last_name" value="{{old('last_name',$user->last_name)}}"></div><div class="field"><label>Password {{$user->exists?'(leave blank to keep current)':''}}</label><input class="input" type="password" name="password" {{$user->exists?'':'required'}}></div><div class="field"><label>Confirm Password</label><input class="input" type="password" name="password_confirmation" {{$user->exists?'':'required'}}></div></div><button class="btn primary">Save User</button></form></div>@endsection
