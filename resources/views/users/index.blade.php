@extends('layouts.app') @section('content')<div class="top"><div><h1>User Management</h1><div class="muted">Manage authorized Owner and Sales Clerk accounts</div></div><div class="who">Owner</div></div><div class="toolbar"><a class="btn primary" href="{{route('users.create')}}">+ Add User</a></div><table class="table"><thead><tr><th>Username</th><th>First Name</th><th>Last Name</th><th>Role</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($users as $u)<tr><td>{{$u->username}}</td><td>{{$u->first_name}}</td><td>{{$u->last_name}}</td><td>{{$u->role==='OWNER'?'Owner':'Sales Clerk'}}</td><td>{{$u->is_active?'Active':'Inactive'}}</td><td><div class="actions"><a class="btn light small" href="{{route('users.edit',$u)}}">Edit</a>@if($u->user_id!==auth()->id())<form method="POST" action="{{route('users.toggle',$u)}}">@csrf @method('PATCH')<button class="btn secondary small">{{$u->is_active?'Deactivate':'Activate'}}</button></form>@endif</div></td></tr>@endforeach</tbody></table><div class="note">Passwords are never displayed in user lists.</div>@endsection
